specVersion: "v1beta"
serviceId: "construction"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "construction-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
